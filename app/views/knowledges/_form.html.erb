<%= form_for(@knowledge) do |f| %>
  <% if @knowledge.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@knowledge.errors.count, "error") %> prohibited this knowledge from being saved:</h2>

      <ul>
      <% @knowledge.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title, placeholder: "title here", style: "width: 400px;" %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description, rows: 20, cols: 50 %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
